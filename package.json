{
  "name": "graviton",
  "version": "2.0.0",
  "description": "Black hole simulator — Zig/WASM + WebGPU + TypeScript",
  "scripts": {
    "build": "node build.js",
    "dev": "node server.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "@webgpu/types": "^0.1.50"
  }
}
